a = (input("enter the number : "))



reversed_number = a[::-1]    # for revesing the number or string int no ave inout 

print(f"the reversed number is {reversed_number}")


#start → Where to begin (default is beginning)

#stop → Where to end (default is end)

#step → How to move (e.g., 1 = forward, -1 = backward)

#So what is [::-1] doing?
#No start → begin at the end

#No stop → go until the beginning

#-1 → step backwards